# Architecture

FlowCite consists of:
- a registry (static items, bindings, injections)
- a collector (runtime usage tracking)
- decorators (scoped_usage, track_item)
- report generators (markdown, text, bibtex, json)
